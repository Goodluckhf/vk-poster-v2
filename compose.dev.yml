version: "3"
services:
  app:
    volumes:
      - ./:/var/www:cached
  php-cron:
    volumes:
      - ./:/var/www:cached
  web:
    volumes: 
      - ./:/var/www:cached
  mysql:
    volumes:
      - mysql-data:/var/lib/mysql:cached
  phpmyadmin: 
    ports:
      - 81:80